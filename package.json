{
	"main": "./build/src/index.js",
	"scripts": {
		"build": "rm -rf ./build/ && npx tsc",
		"start": "cp ./config.json ./build/config.json && node ."
	},
	"dependencies": {
		"discord.js": "^14.6.0",
		"youtube-notifs": "^4.0.5"
	}
}
